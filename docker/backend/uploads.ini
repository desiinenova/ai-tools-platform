; PHP's compiled-in default (upload_max_filesize=2M) silently truncates
; uploads above it before Laravel's request/validation layer ever runs,
; since the base image installs no php.ini. ToolRequest validates images up
; to 4MB (max:4096 KB); these need to stay comfortably above that so PHP's
; ceiling is never the binding constraint — Laravel's validation should be.
upload_max_filesize = 8M
post_max_size = 10M
