# Placeholder config. Real routing (/api/* -> backend, /* -> frontend)
# will be added once both services are functional.
server {
    listen 80;

    location / {
        default_type text/plain;
        return 200 "nginx placeholder - routing not configured yet\n";
    }
}
